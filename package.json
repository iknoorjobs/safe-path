{
  "name": "safeher",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "antd": "^3.9.3",
    "antd-init": "^2.0.6",
    "react-location-picker": "^1.3.0"
  }
}
